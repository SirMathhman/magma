#include <temp.h>
#include <temp.h>
#include <temp.h>
struct Rule {
	struct Rule Rule_new(){
		struct Rule this;
		return this;
	}
	CompileError> Rule_parse(void* _this_){
		struct Rule this = *(struct Rule*) this;
		temp = temp;
	}
	CompileError> Rule_generate(void* _this_){
		struct Rule this = *(struct Rule*) this;
		temp = temp;
	}
};