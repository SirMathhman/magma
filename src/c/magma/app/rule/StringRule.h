#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct StringRule implements Rule {
	(){
		;
		return ;
	}
		;
	(){
		 = *(struct StringRule implements Rule*) this;
		temp = temp;
	}
	(){
		 = *(struct StringRule implements Rule*) this;
		return ;
	}
};