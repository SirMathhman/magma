#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct SuffixRule {
	(){
		;
		return ;
	}
	(){
		 = *(struct SuffixRule*) this;
		temp = temp;
		temp = temp;
	}
};