#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct DivideRule implements Rule {
	(){
		;
	}
		;
		;
	(){
		 = *(struct DivideRule implements Rule*) this;
		temp = temp;
		temp = temp;
	}
	(){
		 = *(struct DivideRule implements Rule*) this;
		temp = temp;
		temp = temp;
		temp = temp;
	}
};