#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct LazyRule implements Rule {
	(){
		;
		return ;
	}
		 = temp;
	(){
		 = *(struct LazyRule implements Rule*) this;
		temp = temp;
	}
	(){
		 = *(struct LazyRule implements Rule*) this;
		temp = temp;
		temp = temp;
	}
	(){
		 = *(struct LazyRule implements Rule*) this;
		temp = temp;
	}
};