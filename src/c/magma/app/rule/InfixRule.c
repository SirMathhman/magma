#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct InfixRule implements Rule {
	(){
		;
		return ;
	}
		;
		;
		;
	(){
		 = *(struct InfixRule implements Rule*) this;
		temp = temp;
		temp = temp;
		temp = temp;
	}
	(){
		 = *(struct InfixRule implements Rule*) this;
		temp = temp;
	}
	(){
		 = *(struct InfixRule implements Rule*) this;
		temp = temp;
	}
};