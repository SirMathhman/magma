#include <temp.h>
#include <temp.h>
#include <temp.h>
struct Result<T, X> {
	struct Result<T, X> Result<T, X>_new(){
		struct Result<T, X> this;
		return this;
	}
	X> Result<T, X>_flatMapValue(void* _this_);
	X> Result<T, X>_mapValue(void* _this_);
	R> Result<T, X>_mapErr(void* _this_);
	R Result<T, X>_match(void* _this_);
	X> Result<T, X>_and(void* _this_);
	R>> Result<T, X>_or(void* _this_);
};