import java.util.Optional;struct EmptyHead<T> implements Head<T>{
	Optional<T> next(){
		return Optional.empty();
	}struct EmptyHead<T> implements Head<T> new(){struct EmptyHead<T> implements Head<T> this;return this;}
}