#include <temp.h>
#include <temp.h>
#include <temp.h>
struct Stream<T> {
	struct Stream<T> Stream<T>_new(){
		struct Stream<T> this;
		return this;
	}
	Optional<R> Stream<T>_foldLeft(void* _this_);
	Stream<R> Stream<T>_map(void* _this_);
};