#include <temp.h>
#include <temp.h>
#include <temp.h>
struct HeadedStream<T> {
	;
	(){
		;
		this.head = head;
		return ;
	}
	(){
		 = *(struct HeadedStream<T>*) this;
		temp = temp;
	}
	(){
		 = *(struct HeadedStream<T>*) this;
		temp = temp;
		temp = temp;
	}
	(){
		 = *(struct HeadedStream<T>*) this;
		temp = temp;
	}
};