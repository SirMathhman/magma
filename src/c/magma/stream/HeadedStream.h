#include <temp.h>
#include <temp.h>
#include <temp.h>
struct HeadedStream<T> {
	Optional<R> foldLeft(){
		return value;
	}
	R foldLeft(){
		temp = temp;
		temp = temp;
	}
	Stream<R> map(){
		return value;
	}
};