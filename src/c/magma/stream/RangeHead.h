#include <temp.h>
struct RangeHead implements Head<Integer> {int extent
		counter  = temp;
	public RangeHead(){
		temp = temp;
	}
	Optional<Integer> next(){
		temp = temp;
		temp = temp;temp++;
		return value;
	}
};