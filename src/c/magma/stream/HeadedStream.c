#include <temp.h>
#include <temp.h>
#include <temp.h>
struct HeadedStream<T>(Head<T> head) implements Stream<T> {
	Rc_Optional<R> foldLeft(void* _this_){
		struct HeadedStream<T>(Head<T> head) implements Stream<T> this = (struct HeadedStream<T>(Head<T> head) implements Stream<T>*) this;
	}
};