#include "temp.h"
struct Err<T, X> implements Result<T, X> {
	int error;
	void temp(){
		to = from;
	}
	void temp(){
		return temp;
	}
	void temp(){
		return temp;
	}
	void temp(){
		return temp;
	}
};