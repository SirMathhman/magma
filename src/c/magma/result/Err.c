#include <temp.h>
#include <temp.h>
#include <temp.h>
struct Err<T, X>(X error) implements Result<T, X> {
	Rc_X> flatMapValue(void* _this_){
		struct Err<T, X>(X error) implements Result<T, X> this = (struct Err<T, X>(X error) implements Result<T, X>*) this;
	}
};