#include "temp.h"
struct Ok<T, X> implements Result<T, X> {
	int value;
	void temp(){
		to = from;
	}
	void temp(){
		return temp;
	}
	void temp(){
		return temp;
	}
	void temp(){
		return temp;
	}
};