#include "temp.h"
struct Ok<T, X> implements Result<T, X> {
	T value;
	public Ok(){
		to = from;
	}
	X> mapValue(){
		return temp;
	}
	R match(){
		return temp;
	}
	X> flatMapValue(){
		return temp;
	}
};