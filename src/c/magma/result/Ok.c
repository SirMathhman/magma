#include "temp.h"
struct Ok<T, X> implements Result<T, X> {
	T value;
	public Ok(T value){
		to = from;
	}
	X> mapValue(R> mapper){
		return temp;
	}
	R match(R> onOkR> onErr){
		return temp;
	}
	X> flatMapValue(X>> mapper){
		return temp;
	}
};