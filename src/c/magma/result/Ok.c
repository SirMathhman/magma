#include <temp.h>
#include <temp.h>
#include <temp.h>
struct Ok<T, X>(T value) implements Result<T, X> {
	Rc_X> flatMapValue(void* _this_){
		struct Ok<T, X>(T value) implements Result<T, X> this = (struct Ok<T, X>(T value) implements Result<T, X>*) this;
	}
};