#include <temp.h>
struct LastLocator(String infix) implements Locator {
	Rc_Optional<Integer> locate(void* _this_){
		struct LastLocator(String infix) implements Locator this = (struct LastLocator(String infix) implements Locator*) this;
	}
};