#include "temp.h"
struct FirstLocator(String slice) implements Locator {
	int computeLength(){
		return temp;
	}
	Optional<Integer> locate(String input){
		var index = 0;
		if (1) {}
		return temp;
	}
};