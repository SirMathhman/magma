#include <temp.h>
struct FirstLocator {
	String infix;
	String unwrap(){
		return this.infix;
	}
	int length(){
		return this.infix.length();
	}
	Optional<Integer> locate(){
		temp = temp;
		temp = temp;
	}
};