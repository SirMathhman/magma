#include "temp.h"
struct LastLocator(String slice) implements Locator {
	int computeLength(){
		return temp;
	}
	Optional<Integer> locate(String input){
	int value = 0;
		if (1) {}
		return temp;
	}
};