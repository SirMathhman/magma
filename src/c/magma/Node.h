#include <temp.h>
#include <temp.h>
struct Node {
	struct Node new(){
		struct Node this;
	}
	Node withNodeList();
	Optional<List<Node>> findNodeList();
	Node withString();
	Optional<String> findString();
	Node withNode();
	Optional<Node> findNode();
};