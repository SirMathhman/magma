#include <temp.h>
#include <temp.h>
struct JavaError {
	Exception e;
	struct JavaError new(Exception e){
	}
	String display(){
		temp = temp;
		this.e.printStackTrace();
		return writer.toString();
	}
};