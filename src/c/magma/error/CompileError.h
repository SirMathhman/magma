#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct CompileError {
	public CompileError(){
		this();
	}
	String display(){
		temp = temp;
	}
	int maxDepth(){
		temp = temp;
	}
	String format(){
		this.children.sort();
		temp = temp;
		return this.message + ": " + this.context + joinedChildren;
	}
};