#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>> {
	struct FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>> FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>>_new(){
		struct FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>> this;
		return this;
	}
	CompileError> FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>>_apply(void* _this_){
		struct FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>> this = *(struct FilterRule(Predicate<String> filter,
                         Function<String, Result<Node, CompileError>> childRule) implements Function<String, Result<Node, CompileError>>*) this;
		temp = temp;
		temp = temp;
	}
};