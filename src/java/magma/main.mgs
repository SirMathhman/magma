import stdlib;
import stdio;

namespace Result {
    enum Type {
        Ok,
        Err
    }

    union Value {
        FILE* okValue;
        errno_t errValue;
    }

    struct Result {
        Type type;
        Value value;
    }
}

int main() {
    return 0;
}