section data {
    first = 100;
    second = 200;
}

section program {
    label __main__ {
        jmp add;
    }

    label main_end {
        halt;
    }

    label add {
        jmp main_end;
    }
}