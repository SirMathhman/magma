section data {
    x = 'a';
}

section program {
    label start {
        jmp test;
    }

    label halt {
        halt;
    }

    label test {
        load x;
        out;
        jmp halt;
    }
}